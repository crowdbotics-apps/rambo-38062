from django.conf import settings
from django.db import models
class Myshedule(models.Model):
    'Generated Model'
    user = models.DateField()
    shedule = models.DateField()

# Create your models here.
